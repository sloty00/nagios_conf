define service {
    use                     generic-service
    host_name               FW ADL Diagnostic
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}

define service {
    use                     generic-service
    host_name               FW Blueshell
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}

define service {
    use                     generic-service
    host_name               FW Cass
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}

define service {
    use                     generic-service
    host_name               FW Clinica PV
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}

define service {
    use                     generic-service
    host_name               FW Confe
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}

define service {
    use                     generic-service
    host_name               FW Electric Works
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}

define service {
    use                     generic-service
    host_name               FW Gore Los Lagos
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}

define service {
    use                     generic-service
    host_name               FW Pasmar
    service_description     Ping
    check_interval          1
    check_command           check_ping!500.0,20%!800.0,60%
}
